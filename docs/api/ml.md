# ml

Machine-learning fatigue-life predictors.

- `features` — feature engineering from geometry, material, and load descriptors.
- `models` — Random Forest and XGBoost predictors with transfer learning support.
- `transfer` — transfer learning that adapts a base predictor trained on one joint type to another with limited data via a residual correction model.

::: feaweld.ml
